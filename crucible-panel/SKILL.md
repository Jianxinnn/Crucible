---
name: crucible-panel
description: Use for open questions that need multiple perspectives before choosing a direction. Choose three disagreement axes, isolate roles when possible, then return the real conflict and smallest next test. Use crucible-grill-you or crucible-grill-me when there is already a fixed plan.
---

Open the decision.

Choose exactly three roles by disagreement axis, not by job title.

Each role answers independently:

- Position:
- Strongest reason:
- Biggest risk:
- What would change my mind:
- Smallest next test:

If isolated agents or external calls are available, run each role in a separate context. Give each role only the topic, its role, and this output format. Do not include prior discussion, other roles, or the intended conclusion.

If isolation is unavailable, simulate the roles in the current context and label the result: "simulated panel, not independent."

After all roles answer, return only:

- Real conflict:
- Decision:
- Do not do:
- Defer:
- Smallest next test:
